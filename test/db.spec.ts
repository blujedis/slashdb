import { assert } from 'chai';
import { Db } from '../src';

describe('Db Class', () => {

  let db: Db;

  before(() => {
    db = new Db();
  });

  it('Should initialize and create a Db instance', () => {

  });


});


 